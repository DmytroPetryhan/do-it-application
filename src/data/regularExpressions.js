const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; //Цей регулярний вираз перевіряє, чи електронна пошта містить не менше одного символа перед символом "@", потім містить символ "@", далі не менше одного символа перед символом ".", символ ".", і, нарешті, не менше одного символа після символу ".".
const passwordRegex =
  /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*])(?=.{8,})/; //Цей регулярний вираз перевіряє, чи пароль містить принаймні одну цифру (\d), принаймні одну маленьку літеру ([a-z]), принаймні одну велику літеру ([A-Z]), принаймні один спеціальний символ ([!@#$%^&*]) та має довжину не менше 8 символів ({8,}).
